# CreateCostType

The cost type. Only `metered` costs are supported; they track usage via events.

## Example Usage

```typescript
import { CreateCostType } from "@paygentic/sdk/models/operations";

let value: CreateCostType = "metered";
```

## Values

```typescript
"metered"
```