# FeatureTypeEnum

The type of feature: `boolean` (on/off), `static` (with config), or `metered` (usage-based).

## Example Usage

```typescript
import { FeatureTypeEnum } from "@paygentic/sdk/models";

let value: FeatureTypeEnum = "boolean";
```

## Values

```typescript
"boolean" | "static" | "metered"
```