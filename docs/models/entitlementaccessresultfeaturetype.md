# EntitlementAccessResultFeatureType

The type of feature: `boolean` (on/off), `static` (with config), or `metered` (usage-based).

## Example Usage

```typescript
import { EntitlementAccessResultFeatureType } from "@paygentic/sdk/models";

let value: EntitlementAccessResultFeatureType = "static";
```

## Values

```typescript
"boolean" | "static" | "metered"
```