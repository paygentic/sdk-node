/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import * as models from "../index.js";

export type ListItemsRequest = {
  /**
   * Filter items by merchant organization id
   */
  merchantId?: string | undefined;
  /**
   * Provider whose external code to resolve (e.g. `salesforce`, `netsuite`). Must be supplied together with `externalId`.
   */
  provider?: string | undefined;
  /**
   * Resolve every live Item mapped to this `(provider, externalId)` — the canonical primary plus any non-primary aliases. The primary Item is returned first and is identifiable by its matching reference's `isPrimary: true`. Empty list on no match. Must be supplied together with `provider`.
   */
  externalId?: string | undefined;
  /**
   * Maximum items to return. In resolution mode (`provider`+`externalId` supplied) pagination is over the de-duplicated resolved Item set, primary Item first.
   */
  limit?: number | undefined;
  /**
   * Zero-based offset for pagination. In resolution mode this paginates the de-duplicated resolved Item set.
   */
  offset?: number | undefined;
};

/**
 * List of items
 */
export type ListItemsResponse = {
  data: Array<models.Item>;
  /**
   * Offset-based pagination response.
   */
  pagination: models.OffsetPagination;
};

/** @internal */
export type ListItemsRequest$Outbound = {
  merchantId?: string | undefined;
  provider?: string | undefined;
  externalId?: string | undefined;
  limit: number;
  offset: number;
};

/** @internal */
export const ListItemsRequest$outboundSchema: z.ZodType<
  ListItemsRequest$Outbound,
  z.ZodTypeDef,
  ListItemsRequest
> = z.object({
  merchantId: z.string().optional(),
  provider: z.string().optional(),
  externalId: z.string().optional(),
  limit: z.number().int().default(50),
  offset: z.number().int().default(0),
});

export function listItemsRequestToJSON(
  listItemsRequest: ListItemsRequest,
): string {
  return JSON.stringify(
    ListItemsRequest$outboundSchema.parse(listItemsRequest),
  );
}

/** @internal */
export const ListItemsResponse$inboundSchema: z.ZodType<
  ListItemsResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  data: z.array(models.Item$inboundSchema),
  pagination: models.OffsetPagination$inboundSchema,
});

export function listItemsResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListItemsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListItemsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListItemsResponse' from JSON`,
  );
}
