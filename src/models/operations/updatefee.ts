/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";

export type UpdateFeeRequestBody = {
  /**
   * Revised explanation of what the fee represents.
   */
  description?: string | undefined;
  /**
   * Updated label for the fee.
   */
  name?: string | undefined;
  /**
   * Optional item tag, used to map this fee's invoice lines to an external accounting/tax identity. Send a new id to re-tag — the item must be filed under this charge's own product, and an archived item is rejected. An item from another product is refused with `ITEM_PRODUCT_MISMATCH`: a tag is an accounting grouping and does not move the charge between products, and no field here could move it back. Re-file the item to move every charge anchored to it together. Send `null` to untag. Every line item whose invoice has not closed reports this charge's current tag, so a re-tag takes effect on the bill in progress and on any generated ahead of it — no further action, and no window to wait for. Lines on a closed invoice keep the item recorded at close and never move. Un-tagging works the same way: those lines report no item.
   */
  itemId?: string | null | undefined;
};

export type UpdateFeeRequest = {
  /**
   * The unique identifier of the fee
   */
  id: string;
  requestBody: UpdateFeeRequestBody;
};

/** @internal */
export type UpdateFeeRequestBody$Outbound = {
  description?: string | undefined;
  name?: string | undefined;
  itemId?: string | null | undefined;
};

/** @internal */
export const UpdateFeeRequestBody$outboundSchema: z.ZodType<
  UpdateFeeRequestBody$Outbound,
  z.ZodTypeDef,
  UpdateFeeRequestBody
> = z.object({
  description: z.string().optional(),
  name: z.string().optional(),
  itemId: z.nullable(z.string()).optional(),
});

export function updateFeeRequestBodyToJSON(
  updateFeeRequestBody: UpdateFeeRequestBody,
): string {
  return JSON.stringify(
    UpdateFeeRequestBody$outboundSchema.parse(updateFeeRequestBody),
  );
}

/** @internal */
export type UpdateFeeRequest$Outbound = {
  id: string;
  RequestBody: UpdateFeeRequestBody$Outbound;
};

/** @internal */
export const UpdateFeeRequest$outboundSchema: z.ZodType<
  UpdateFeeRequest$Outbound,
  z.ZodTypeDef,
  UpdateFeeRequest
> = z.object({
  id: z.string(),
  requestBody: z.lazy(() => UpdateFeeRequestBody$outboundSchema),
}).transform((v) => {
  return remap$(v, {
    requestBody: "RequestBody",
  });
});

export function updateFeeRequestToJSON(
  updateFeeRequest: UpdateFeeRequest,
): string {
  return JSON.stringify(
    UpdateFeeRequest$outboundSchema.parse(updateFeeRequest),
  );
}
