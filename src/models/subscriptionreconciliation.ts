/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  ReconciledFeatureAdded,
  ReconciledFeatureAdded$inboundSchema,
} from "./reconciledfeatureadded.js";
import {
  ReconciledFeatureFailed,
  ReconciledFeatureFailed$inboundSchema,
} from "./reconciledfeaturefailed.js";
import {
  ReconciledFeatureRemoved,
  ReconciledFeatureRemoved$inboundSchema,
} from "./reconciledfeatureremoved.js";
import {
  ReconciledFeatureSkipped,
  ReconciledFeatureSkipped$inboundSchema,
} from "./reconciledfeatureskipped.js";

export const SubscriptionReconciliationObject = {
  SubscriptionReconciliation: "subscriptionReconciliation",
} as const;
export type SubscriptionReconciliationObject = ClosedEnum<
  typeof SubscriptionReconciliationObject
>;

export type Features = {
  added: Array<ReconciledFeatureAdded>;
  /**
   * Features the subscription already had an active entitlement for — left unchanged.
   */
  skipped: Array<ReconciledFeatureSkipped>;
  /**
   * Features no longer on the plan: their entitlement was canceled and grants voided so the subscription matches the plan's current features.
   */
  removed: Array<ReconciledFeatureRemoved>;
  /**
   * Features that could not be fully provisioned. Never billed by this call.
   */
  failed: Array<ReconciledFeatureFailed>;
};

export type SubscriptionReconciliationLineItems = {
  /**
   * Number of features newly billed on the subscription by this reconciliation. A dry-run preview returns the same count as the applied run.
   */
  created: number;
  /**
   * Number of features whose billing was removed from the subscription's upcoming (not-yet-invoiced) invoice because their price is no longer on the plan.
   */
  removed: number;
  /**
   * True when entitlements were provisioned successfully but the line-item synchronization step failed. Safe to retry.
   */
  syncFailed?: boolean | undefined;
};

export type SubscriptionReconciliation = {
  object: SubscriptionReconciliationObject;
  /**
   * True when this reconciliation was a preview — no entitlement, grant, or line item was created.
   */
  dryRun: boolean;
  features: Features;
  lineItems: SubscriptionReconciliationLineItems;
};

/** @internal */
export const SubscriptionReconciliationObject$inboundSchema: z.ZodNativeEnum<
  typeof SubscriptionReconciliationObject
> = z.nativeEnum(SubscriptionReconciliationObject);

/** @internal */
export const Features$inboundSchema: z.ZodType<
  Features,
  z.ZodTypeDef,
  unknown
> = z.object({
  added: z.array(ReconciledFeatureAdded$inboundSchema),
  skipped: z.array(ReconciledFeatureSkipped$inboundSchema),
  removed: z.array(ReconciledFeatureRemoved$inboundSchema),
  failed: z.array(ReconciledFeatureFailed$inboundSchema),
});

export function featuresFromJSON(
  jsonString: string,
): SafeParseResult<Features, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Features$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Features' from JSON`,
  );
}

/** @internal */
export const SubscriptionReconciliationLineItems$inboundSchema: z.ZodType<
  SubscriptionReconciliationLineItems,
  z.ZodTypeDef,
  unknown
> = z.object({
  created: z.number().int(),
  removed: z.number().int(),
  syncFailed: z.boolean().optional(),
});

export function subscriptionReconciliationLineItemsFromJSON(
  jsonString: string,
): SafeParseResult<SubscriptionReconciliationLineItems, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      SubscriptionReconciliationLineItems$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SubscriptionReconciliationLineItems' from JSON`,
  );
}

/** @internal */
export const SubscriptionReconciliation$inboundSchema: z.ZodType<
  SubscriptionReconciliation,
  z.ZodTypeDef,
  unknown
> = z.object({
  object: SubscriptionReconciliationObject$inboundSchema,
  dryRun: z.boolean(),
  features: z.lazy(() => Features$inboundSchema),
  lineItems: z.lazy(() => SubscriptionReconciliationLineItems$inboundSchema),
});

export function subscriptionReconciliationFromJSON(
  jsonString: string,
): SafeParseResult<SubscriptionReconciliation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SubscriptionReconciliation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SubscriptionReconciliation' from JSON`,
  );
}
