/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export const FeeObject = {
  Fee: "fee",
} as const;
export type FeeObject = ClosedEnum<typeof FeeObject>;

export type Fee = {
  id: string;
  object: FeeObject;
  name: string;
  description: string;
  /**
   * Unique identifier for an organization
   */
  merchantId: string;
  /**
   * Unique identifier for a product
   */
  productId: string;
  createdAt: Date;
  updatedAt: Date;
};

/** @internal */
export const FeeObject$inboundSchema: z.ZodNativeEnum<typeof FeeObject> = z
  .nativeEnum(FeeObject);

/** @internal */
export const Fee$inboundSchema: z.ZodType<Fee, z.ZodTypeDef, unknown> = z
  .object({
    id: z.string(),
    object: FeeObject$inboundSchema.default("fee"),
    name: z.string(),
    description: z.string(),
    merchantId: z.string(),
    productId: z.string(),
    createdAt: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
    updatedAt: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
  });

export function feeFromJSON(
  jsonString: string,
): SafeParseResult<Fee, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Fee$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Fee' from JSON`,
  );
}
