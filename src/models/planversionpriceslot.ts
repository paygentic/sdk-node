/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { PriceFeature, PriceFeature$inboundSchema } from "./pricefeature.js";

export const PlanVersionPriceSlotObject = {
  Price: "price",
} as const;
export type PlanVersionPriceSlotObject = ClosedEnum<
  typeof PlanVersionPriceSlotObject
>;

export const PlanVersionPriceSlotModel = {
  Standard: "standard",
  Dynamic: "dynamic",
  Volume: "volume",
  Percentage: "percentage",
} as const;
export type PlanVersionPriceSlotModel = ClosedEnum<
  typeof PlanVersionPriceSlotModel
>;

export const PlanVersionPriceSlotPaymentTerm = {
  InArrears: "in_arrears",
  InAdvance: "in_advance",
} as const;
export type PlanVersionPriceSlotPaymentTerm = ClosedEnum<
  typeof PlanVersionPriceSlotPaymentTerm
>;

/**
 * One price slot on a plan version. Every `Price` field is present, plus `priceDeleted` layered on top.
 */
export type PlanVersionPriceSlot = {
  /**
   * Unique identifier for a price
   */
  id: string;
  object: PlanVersionPriceSlotObject;
  /**
   * Unique identifier for an organization
   */
  merchantId: string;
  billableMetricId?: string | undefined;
  /**
   * The unique identifier for the fee referred to by this price
   */
  feeId?: string | undefined;
  /**
   * ISO 8601 duration for billing frequency (e.g., P1M for monthly)
   */
  billingCadence?: string | null | undefined;
  createdAt: Date;
  currency?: string | undefined;
  description?: string | undefined;
  invoiceDisplayName: string;
  model: PlanVersionPriceSlotModel;
  paymentTerm: PlanVersionPriceSlotPaymentTerm;
  properties: { [k: string]: any };
  unitAmount?: string | undefined;
  updatedAt: Date;
  /**
   * Features associated with this price
   */
  features?: Array<PriceFeature> | undefined;
  /**
   * When true, grants applied to a subscription will discount usage charged by this price. Only supported for standard metered prices.
   */
  grantDiscountEnabled: boolean;
  /**
   * A fixed amount owed whole rather than a per-period rate. An obligation is not prorated over a partial first period: when a subscription starts before its billing anchor, no truncated stub is billed and the first charge is the full amount at the next anchor. An obligation also refuses an interval boundary that falls strictly inside one of its own billing periods, since part of an amount owed whole is not a thing to bill. Defaults to false, which is a rate and is today's behaviour for every price. Not supported on a metered price, whose amount resolves from usage at close.
   */
  isObligation: boolean;
  /**
   * Quantity used when generating invoice line items for this price. Total per period = quantity × unitPrice. Only supported for fee prices; metered prices derive quantity from usage. Defaults to 1.
   */
  quantity: number;
  /**
   * True when the underlying price this slot references has been soft-deleted.
   */
  priceDeleted: boolean;
};

/** @internal */
export const PlanVersionPriceSlotObject$inboundSchema: z.ZodNativeEnum<
  typeof PlanVersionPriceSlotObject
> = z.nativeEnum(PlanVersionPriceSlotObject);

/** @internal */
export const PlanVersionPriceSlotModel$inboundSchema: z.ZodNativeEnum<
  typeof PlanVersionPriceSlotModel
> = z.nativeEnum(PlanVersionPriceSlotModel);

/** @internal */
export const PlanVersionPriceSlotPaymentTerm$inboundSchema: z.ZodNativeEnum<
  typeof PlanVersionPriceSlotPaymentTerm
> = z.nativeEnum(PlanVersionPriceSlotPaymentTerm);

/** @internal */
export const PlanVersionPriceSlot$inboundSchema: z.ZodType<
  PlanVersionPriceSlot,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  object: PlanVersionPriceSlotObject$inboundSchema.default("price"),
  merchantId: z.string(),
  billableMetricId: z.string().optional(),
  feeId: z.string().optional(),
  billingCadence: z.nullable(z.string()).optional(),
  createdAt: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  currency: z.string().optional(),
  description: z.string().optional(),
  invoiceDisplayName: z.string(),
  model: PlanVersionPriceSlotModel$inboundSchema,
  paymentTerm: PlanVersionPriceSlotPaymentTerm$inboundSchema,
  properties: z.record(z.any()),
  unitAmount: z.string().optional(),
  updatedAt: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  features: z.array(PriceFeature$inboundSchema).optional(),
  grantDiscountEnabled: z.boolean().default(false),
  isObligation: z.boolean().default(false),
  quantity: z.number().int().default(1),
  priceDeleted: z.boolean(),
});

export function planVersionPriceSlotFromJSON(
  jsonString: string,
): SafeParseResult<PlanVersionPriceSlot, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlanVersionPriceSlot$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlanVersionPriceSlot' from JSON`,
  );
}
