/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type TestClock = {
  /**
   * Unique identifier for the test clock
   */
  id: string;
  /**
   * When the test clock was created
   */
  createdAt: Date;
  /**
   * The simulated current time of the test clock
   */
  currentTime: Date;
  /**
   * When the test clock was deleted (if applicable)
   */
  deletedAt?: Date | null | undefined;
  /**
   * Description of the test clock's purpose
   */
  description?: string | null | undefined;
  /**
   * The merchant organization that owns this test clock
   */
  merchantId: string;
  /**
   * Name of the test clock
   */
  name?: string | null | undefined;
  /**
   * When the test clock was last updated
   */
  updatedAt: Date;
};

/** @internal */
export const TestClock$inboundSchema: z.ZodType<
  TestClock,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  createdAt: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  currentTime: z.string().datetime({ offset: true }).transform(v =>
    new Date(v)
  ),
  deletedAt: z.nullable(
    z.string().datetime({ offset: true }).transform(v => new Date(v)),
  ).optional(),
  description: z.nullable(z.string()).optional(),
  merchantId: z.string(),
  name: z.nullable(z.string()).optional(),
  updatedAt: z.string().datetime({ offset: true }).transform(v => new Date(v)),
});

export function testClockFromJSON(
  jsonString: string,
): SafeParseResult<TestClock, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TestClock$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TestClock' from JSON`,
  );
}
