/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export const ProductObject = {
  Product: "product",
} as const;
export type ProductObject = ClosedEnum<typeof ProductObject>;

export type Product = {
  /**
   * Unique identifier for a product
   */
  id: string;
  object: ProductObject;
  createdAt: Date;
  description: string;
  /**
   * Unique identifier for an organization
   */
  merchantId: string;
  metadata: { [k: string]: string };
  name: string;
  updatedAt: Date;
};

/** @internal */
export const ProductObject$inboundSchema: z.ZodNativeEnum<
  typeof ProductObject
> = z.nativeEnum(ProductObject);

/** @internal */
export const Product$inboundSchema: z.ZodType<Product, z.ZodTypeDef, unknown> =
  z.object({
    id: z.string(),
    object: ProductObject$inboundSchema.default("product"),
    createdAt: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
    description: z.string(),
    merchantId: z.string(),
    metadata: z.record(z.string()),
    name: z.string(),
    updatedAt: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
  });

export function productFromJSON(
  jsonString: string,
): SafeParseResult<Product, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Product$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Product' from JSON`,
  );
}
