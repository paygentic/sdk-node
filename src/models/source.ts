/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export const SourceObject = {
  Source: "source",
} as const;
export type SourceObject = ClosedEnum<typeof SourceObject>;

export const ProcessingMode = {
  Automatic: "automatic",
  Manual: "manual",
} as const;
export type ProcessingMode = ClosedEnum<typeof ProcessingMode>;

export const SourceType = {
  StripeRevenue: "stripe_revenue",
} as const;
export type SourceType = ClosedEnum<typeof SourceType>;

export type Source = {
  /**
   * Unique identifier for a source
   */
  id: string;
  object: SourceObject;
  config?: { [k: string]: any } | undefined;
  createdAt: Date;
  description?: string | undefined;
  enabled: boolean;
  /**
   * Unique identifier for an organization
   */
  merchantId: string;
  metadata?: { [k: string]: any } | undefined;
  name: string;
  /**
   * Unique identifier for a plan
   */
  planId: string;
  processingMode: ProcessingMode;
  type: SourceType;
  updatedAt: Date;
};

/** @internal */
export const SourceObject$inboundSchema: z.ZodNativeEnum<typeof SourceObject> =
  z.nativeEnum(SourceObject);

/** @internal */
export const ProcessingMode$inboundSchema: z.ZodNativeEnum<
  typeof ProcessingMode
> = z.nativeEnum(ProcessingMode);

/** @internal */
export const SourceType$inboundSchema: z.ZodNativeEnum<typeof SourceType> = z
  .nativeEnum(SourceType);

/** @internal */
export const Source$inboundSchema: z.ZodType<Source, z.ZodTypeDef, unknown> = z
  .object({
    id: z.string(),
    object: SourceObject$inboundSchema,
    config: z.record(z.any()).optional(),
    createdAt: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
    description: z.string().optional(),
    enabled: z.boolean(),
    merchantId: z.string(),
    metadata: z.record(z.any()).optional(),
    name: z.string(),
    planId: z.string(),
    processingMode: ProcessingMode$inboundSchema,
    type: SourceType$inboundSchema,
    updatedAt: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
  });

export function sourceFromJSON(
  jsonString: string,
): SafeParseResult<Source, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Source$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Source' from JSON`,
  );
}
