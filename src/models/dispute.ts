/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Decimal as Decimal$ } from "../types/decimal.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export const DisputeObject = {
  Dispute: "dispute",
} as const;
export type DisputeObject = ClosedEnum<typeof DisputeObject>;

/**
 * Current status of the dispute
 */
export const DisputeStatus = {
  Pending: "pending",
  Accepted: "accepted",
  Declined: "declined",
} as const;
/**
 * Current status of the dispute
 */
export type DisputeStatus = ClosedEnum<typeof DisputeStatus>;

export type Dispute = {
  /**
   * Unique identifier for a dispute
   */
  id: string;
  object: DisputeObject;
  createdAt: Date;
  /**
   * Customer who filed the dispute
   */
  customerId: string;
  /**
   * Customer's reason for the dispute
   */
  customerMessage: string;
  /**
   * Disputed charge amount in decimal dollar format. Sample values: '2.50' equals $2.50 disputed, '15.00' equals $15.00 disputed, '0.50' equals $0.50 disputed
   */
  disputedAmount: Decimal$ | number;
  /**
   * Unique identifier for an organization
   */
  merchantId: string;
  /**
   * Merchant's response to the dispute
   */
  merchantMessage?: string | null | undefined;
  /**
   * When the dispute was resolved
   */
  resolvedAt?: Date | null | undefined;
  /**
   * Current status of the dispute
   */
  status: DisputeStatus;
  updatedAt: Date;
  /**
   * The usage event being disputed
   */
  usageEventId: string;
};

/** @internal */
export const DisputeObject$inboundSchema: z.ZodNativeEnum<
  typeof DisputeObject
> = z.nativeEnum(DisputeObject);

/** @internal */
export const DisputeStatus$inboundSchema: z.ZodNativeEnum<
  typeof DisputeStatus
> = z.nativeEnum(DisputeStatus);

/** @internal */
export const Dispute$inboundSchema: z.ZodType<Dispute, z.ZodTypeDef, unknown> =
  z.object({
    id: z.string(),
    object: DisputeObject$inboundSchema.default("dispute"),
    createdAt: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
    customerId: z.string(),
    customerMessage: z.string(),
    disputedAmount: z.string().transform(v => new Decimal$(v)),
    merchantId: z.string(),
    merchantMessage: z.nullable(z.string()).optional(),
    resolvedAt: z.nullable(
      z.string().datetime({ offset: true }).transform(v => new Date(v)),
    ).optional(),
    status: DisputeStatus$inboundSchema,
    updatedAt: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
    usageEventId: z.string(),
  });

export function disputeFromJSON(
  jsonString: string,
): SafeParseResult<Dispute, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Dispute$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Dispute' from JSON`,
  );
}
