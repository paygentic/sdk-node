/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Coded failure reason. `grant_mint_failed` means the entitlement was created but its initial metered grant could not be minted; re-running this reconciliation retries the mint.
 */
export const Reason = {
  EntitlementFailed: "entitlement_failed",
  GrantMintFailed: "grant_mint_failed",
} as const;
/**
 * Coded failure reason. `grant_mint_failed` means the entitlement was created but its initial metered grant could not be minted; re-running this reconciliation retries the mint.
 */
export type Reason = ClosedEnum<typeof Reason>;

export type ReconciledFeatureFailed = {
  featureId: string;
  featureKey: string;
  /**
   * Coded failure reason. `grant_mint_failed` means the entitlement was created but its initial metered grant could not be minted; re-running this reconciliation retries the mint.
   */
  reason: Reason;
};

/** @internal */
export const Reason$inboundSchema: z.ZodNativeEnum<typeof Reason> = z
  .nativeEnum(Reason);

/** @internal */
export const ReconciledFeatureFailed$inboundSchema: z.ZodType<
  ReconciledFeatureFailed,
  z.ZodTypeDef,
  unknown
> = z.object({
  featureId: z.string(),
  featureKey: z.string(),
  reason: Reason$inboundSchema,
});

export function reconciledFeatureFailedFromJSON(
  jsonString: string,
): SafeParseResult<ReconciledFeatureFailed, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReconciledFeatureFailed$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReconciledFeatureFailed' from JSON`,
  );
}
