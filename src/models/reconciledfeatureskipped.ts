/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type ReconciledFeatureSkipped = {
  featureId: string;
  featureKey: string;
};

/** @internal */
export const ReconciledFeatureSkipped$inboundSchema: z.ZodType<
  ReconciledFeatureSkipped,
  z.ZodTypeDef,
  unknown
> = z.object({
  featureId: z.string(),
  featureKey: z.string(),
});

export function reconciledFeatureSkippedFromJSON(
  jsonString: string,
): SafeParseResult<ReconciledFeatureSkipped, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReconciledFeatureSkipped$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReconciledFeatureSkipped' from JSON`,
  );
}
