/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import * as operations from "../operations/index.js";
import { PaygenticError } from "./paygenticerror.js";

/**
 * Price cannot be restructured because a subscription still bills it directly
 */
export type UpdatePriceConflictErrorData = {
  error?: string | undefined;
  message: string;
  /**
   * Machine-readable conflict code. Open set: a new code may appear, so branch on the ones you handle and treat anything else as an unhandled conflict.
   */
  code: string;
  details?: operations.UpdatePriceDetails | undefined;
};

/**
 * Price cannot be restructured because a subscription still bills it directly
 */
export class UpdatePriceConflictError extends PaygenticError {
  error?: string | undefined;
  /**
   * Machine-readable conflict code. Open set: a new code may appear, so branch on the ones you handle and treat anything else as an unhandled conflict.
   */
  code: string;
  details?: operations.UpdatePriceDetails | undefined;

  /** The original data that was passed to this error instance. */
  data$: UpdatePriceConflictErrorData;

  constructor(
    err: UpdatePriceConflictErrorData,
    httpMeta: { response: Response; request: Request; body: string },
  ) {
    const message = err.message || `API error occurred: ${JSON.stringify(err)}`;
    super(message, httpMeta);
    this.data$ = err;
    if (err.error != null) this.error = err.error;
    this.code = err.code;
    if (err.details != null) this.details = err.details;

    this.name = "UpdatePriceConflictError";
  }
}

/** @internal */
export const UpdatePriceConflictError$inboundSchema: z.ZodType<
  UpdatePriceConflictError,
  z.ZodTypeDef,
  unknown
> = z.object({
  error: z.string().optional(),
  message: z.string(),
  code: z.string(),
  details: z.lazy(() => operations.UpdatePriceDetails$inboundSchema).optional(),
  request$: z.instanceof(Request),
  response$: z.instanceof(Response),
  body$: z.string(),
})
  .transform((v) => {
    return new UpdatePriceConflictError(v, {
      request: v.request$,
      response: v.response$,
      body: v.body$,
    });
  });
