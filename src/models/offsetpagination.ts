/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Offset-based pagination response.
 */
export type OffsetPagination = {
  /**
   * Number of items returned in the current page.
   */
  limit?: number | undefined;
  /**
   * Number of items skipped.
   */
  offset?: number | undefined;
  /**
   * Total number of items available.
   */
  total?: number | undefined;
};

/** @internal */
export const OffsetPagination$inboundSchema: z.ZodType<
  OffsetPagination,
  z.ZodTypeDef,
  unknown
> = z.object({
  limit: z.number().int().optional(),
  offset: z.number().int().optional(),
  total: z.number().int().optional(),
});

export function offsetPaginationFromJSON(
  jsonString: string,
): SafeParseResult<OffsetPagination, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OffsetPagination$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OffsetPagination' from JSON`,
  );
}
