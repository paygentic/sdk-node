/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * What this provider does with an item's external codes.
 *
 * @remarks
 *
 * Two independent capabilities, not one direction: the behaviours are not alternatives. An integration can resolve an incoming item code *and* be sent a selected one on a different path, so a single `inbound`/`outbound` value would have to misdescribe it or forbid one of its operations.
 *
 * A capability that is not declared is unavailable rather than inferred. Declaring one does not make a provider connectable — that still requires its credentials and connection lifecycle.
 */
export type MappingCapabilities = {
  /**
   * A code arriving from this provider must name exactly one of the merchant's items. The lookup key is the code, so several codes may point at one item while each code resolves to one — which is why a code recorded for this purpose is claimed, and a second item claiming it is a conflict.
   */
  resolvesItemCodes: boolean;
  /**
   * A consumer needs one of an item's codes to send to this provider, and takes the item's designated one (`isDefault`). Because the lookup key is the item, several items may share one code — a chart of accounts exists so that many things sold roll into one account.
   */
  sendsItemCodes: boolean;
};

/** @internal */
export const MappingCapabilities$inboundSchema: z.ZodType<
  MappingCapabilities,
  z.ZodTypeDef,
  unknown
> = z.object({
  resolvesItemCodes: z.boolean(),
  sendsItemCodes: z.boolean(),
});

export function mappingCapabilitiesFromJSON(
  jsonString: string,
): SafeParseResult<MappingCapabilities, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MappingCapabilities$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MappingCapabilities' from JSON`,
  );
}
