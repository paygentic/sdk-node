/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type PlanCreditAllocation = {
  /**
   * Unique identifier for a pricing unit
   */
  pricingUnitId: string;
  /**
   * Number of credits minted into the customer's pool for this pricing unit each time the allocation is provisioned. Must be a positive number.
   */
  amount: number;
  /**
   * ISO 8601 duration (e.g. 'P1M') controlling how often the allocation's credits are re-minted on the customer's pool. Recurrence is opt-in: when omitted, the allocation is minted once at subscription activation and never refills — even on a recurring plan. Set this field explicitly for credits that should replenish each period.
   */
  recurrencePeriod?: string | undefined;
};

/** @internal */
export const PlanCreditAllocation$inboundSchema: z.ZodType<
  PlanCreditAllocation,
  z.ZodTypeDef,
  unknown
> = z.object({
  pricingUnitId: z.string(),
  amount: z.number(),
  recurrencePeriod: z.string().optional(),
});
/** @internal */
export type PlanCreditAllocation$Outbound = {
  pricingUnitId: string;
  amount: number;
  recurrencePeriod?: string | undefined;
};

/** @internal */
export const PlanCreditAllocation$outboundSchema: z.ZodType<
  PlanCreditAllocation$Outbound,
  z.ZodTypeDef,
  PlanCreditAllocation
> = z.object({
  pricingUnitId: z.string(),
  amount: z.number(),
  recurrencePeriod: z.string().optional(),
});

export function planCreditAllocationToJSON(
  planCreditAllocation: PlanCreditAllocation,
): string {
  return JSON.stringify(
    PlanCreditAllocation$outboundSchema.parse(planCreditAllocation),
  );
}
export function planCreditAllocationFromJSON(
  jsonString: string,
): SafeParseResult<PlanCreditAllocation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlanCreditAllocation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlanCreditAllocation' from JSON`,
  );
}
