/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export const PlanVersionSummaryObject = {
  PlanVersion: "plan_version",
} as const;
export type PlanVersionSummaryObject = ClosedEnum<
  typeof PlanVersionSummaryObject
>;

/**
 * Lifecycle status of the version.
 */
export const PlanVersionSummaryStatus = {
  Draft: "draft",
  Published: "published",
  Archived: "archived",
} as const;
/**
 * Lifecycle status of the version.
 */
export type PlanVersionSummaryStatus = ClosedEnum<
  typeof PlanVersionSummaryStatus
>;

export type PlanVersionSummary = {
  /**
   * Unique identifier for a plan version
   */
  id: string;
  object: PlanVersionSummaryObject;
  /**
   * Monotonic version number within the plan, starting at 1.
   */
  versionNumber: number;
  /**
   * Lifecycle status of the version.
   */
  status: PlanVersionSummaryStatus;
  /**
   * When this version was published. Absent for draft versions.
   */
  publishedAt?: Date | undefined;
  /**
   * Whether this version is the plan's current default (live) version.
   */
  isDefault: boolean;
  /**
   * Number of committed-status subscriptions (active or pending payment) pinned to this version at creation time. This is a count of pins recorded when each subscription was created, not a live-billing cohort: for billing, subscriptions follow the plan's current default version regardless of the version they were pinned to.
   */
  subscriptionCount: number;
};

/** @internal */
export const PlanVersionSummaryObject$inboundSchema: z.ZodNativeEnum<
  typeof PlanVersionSummaryObject
> = z.nativeEnum(PlanVersionSummaryObject);

/** @internal */
export const PlanVersionSummaryStatus$inboundSchema: z.ZodNativeEnum<
  typeof PlanVersionSummaryStatus
> = z.nativeEnum(PlanVersionSummaryStatus);

/** @internal */
export const PlanVersionSummary$inboundSchema: z.ZodType<
  PlanVersionSummary,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  object: PlanVersionSummaryObject$inboundSchema,
  versionNumber: z.number().int(),
  status: PlanVersionSummaryStatus$inboundSchema,
  publishedAt: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  isDefault: z.boolean(),
  subscriptionCount: z.number().int(),
});

export function planVersionSummaryFromJSON(
  jsonString: string,
): SafeParseResult<PlanVersionSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlanVersionSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlanVersionSummary' from JSON`,
  );
}
