/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Indicates whether the consumer address is valid for tax calculation when using Paygentic Tax. If valid=false, tax calculation will be skipped and internal invoice flow with default tax rate will be used.
 */
export type ValidTaxAddress = {
  /**
   * Validation message if address is invalid. Sample values: 'Country is required for tax calculation', 'Invalid postal code format', 'State/province required for this country'
   */
  message?: string | undefined;
  /**
   * Whether the consumer address is valid for tax calculation when using Paygentic Tax
   */
  valid: boolean;
};

/** @internal */
export const ValidTaxAddress$inboundSchema: z.ZodType<
  ValidTaxAddress,
  z.ZodTypeDef,
  unknown
> = z.object({
  message: z.string().optional(),
  valid: z.boolean(),
});

export function validTaxAddressFromJSON(
  jsonString: string,
): SafeParseResult<ValidTaxAddress, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ValidTaxAddress$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ValidTaxAddress' from JSON`,
  );
}
