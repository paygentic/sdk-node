/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type ReconciledFeatureAdded = {
  featureId: string;
  featureKey: string;
  /**
   * False when this feature's price never produces a recurring line item (a credit-denominated price, or an already-elapsed one-time charge) — the feature is entitled but not billed by this reconciliation.
   */
  billed: boolean;
};

/** @internal */
export const ReconciledFeatureAdded$inboundSchema: z.ZodType<
  ReconciledFeatureAdded,
  z.ZodTypeDef,
  unknown
> = z.object({
  featureId: z.string(),
  featureKey: z.string(),
  billed: z.boolean(),
});

export function reconciledFeatureAddedFromJSON(
  jsonString: string,
): SafeParseResult<ReconciledFeatureAdded, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReconciledFeatureAdded$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReconciledFeatureAdded' from JSON`,
  );
}
